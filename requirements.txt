## ✅ requirements.txt
streamlit
python-dotenv
PyMuPDF
langchain
langchain-community
langchain-huggingface
huggingface_hub
sentence-transformers
faiss-cpu
typing_extensions  # for Python 3.11+


## 📝 Why Each One Is Included
| Package                 | Why it's needed                                |
| ----------------------- | ---------------------------------------------- |
| `streamlit`             | For the web app UI                             |
| `python-dotenv`         | For loading `.env` variables                   |
| `PyMuPDF`               | To extract text from PDFs                      |
| `langchain`             | Core language model framework                  |
| `langchain-community`   | You’re using `PyMuPDFLoader` from this module  |
| `langchain-huggingface` | You’re using `HuggingFaceEmbeddings`           |
| `huggingface_hub`       | For logging in and using the `InferenceClient` |
| `sentence-transformers` | For the embedding model you specified          |
| `faiss-cpu`             | For vector search and similarity retrieval     |
| `typing_extensions`     | Required for compatibility with Python 3.11+   |

